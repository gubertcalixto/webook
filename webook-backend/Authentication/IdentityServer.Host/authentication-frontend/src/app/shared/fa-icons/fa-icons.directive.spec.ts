import { FaIconsDirective } from './fa-icons.directive';

describe('FaIconsDirective', () => {
  it('should create an instance', () => {
    const directive = new FaIconsDirective();
    expect(directive).toBeTruthy();
  });
});
